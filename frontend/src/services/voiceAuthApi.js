import { healthCheck, enrollUser, verifyUser, logAlert, trainModels } from './api';

export const voiceAuthAPI = {
  healthCheck,
  enrollUser,
  verifyUser,
  logAlert,
  trainModels,
};

export default voiceAuthAPI;